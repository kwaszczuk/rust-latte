[package]
name = "compiler"
version = "0.1.0"
authors = ["Karol Waszczuk <waszczukarol@gmail.com>"]
edition = "2018"

[dependencies]
base = { path = "../base" }
parser = { path = "../parser" }

[lib]
name = "compiler"
path = "src/lib.rs"
