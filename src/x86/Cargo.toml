[package]
name = "x86"
version = "0.1.0"
authors = ["Karol Waszczuk <waszczukarol@gmail.com>"]
edition = "2018"

[dependencies]
base = { path = "../base" }
llvm = { path = "../llvm" }

[lib]
name = "x86"
path = "src/lib.rs"

[features]
no-registers-allocation = []

default=[]
